export const enum ERoutes {
    login = '/login',
    register = '/register',
    inventory = '/inventory',
    plan = '/plan',
    userInventory = '/user-inventory',
    userRequest = '/user-request',
    request = '/request'
}